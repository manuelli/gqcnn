# dataset params
dataset_path: /nfs/diskstation/projects/dex-net/parallel_jaws/datasets/updated_manuelli/grasps

image_mode: tf_depth_ims
data_format: parallel_jaw
metric_name: grasp_metrics
metric_thresh: 0.75
gripper_width_m: 0.05

aux_fields:
  - grasp_metrics
  - collisions

# gqcnn params
model_dir: /nfs/diskstation/projects/dex-net/parallel_jaws/models/manuelli/model_izmouiokft

# vis params
datapoint_type: false_positive # which datapoints to visualize, possible values are true positive, false positive, true negative, false negative
display_image_type: depth
font_size: 15
samples_per_object: 1